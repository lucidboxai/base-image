bcrypt
uvicorn>=0.34
fastapi>=0.115
# The apps call TemplateResponse(request, name, context) — the signature added
# in Starlette 0.29. Pulled in transitively by fastapi; pinned explicitly so the
# requirement is visible rather than implied.
starlette>=0.29
jinja2>=3.1
jinja_partials
python-multipart
websockets